@model IEnumerable<QuanLyDeTai.Models.GiangVienPhanBien>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.maGiangVien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DeTai.tenDeTai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.maGiangVien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeTai.tenDeTai)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.maPhanBien }) |
            @Html.ActionLink("Details", "Details", new { id=item.maPhanBien }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.maPhanBien })
        </td>
    </tr>
}

</table>
